export class Person {
    name: string;
    year_of_birth: number;
    address: string;
  
    constructor(name: string, year_of_birth: number, address: string) {
      this.name = name;
      this.year_of_birth = year_of_birth;
      this.address = address;
    }
}